apiVersion: apps/v1
kind: Deployment
metadata:
  name: teedy-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: teedy-app
  template:
    metadata:
      labels:
        app: teedy-app
    spec:
      containers:
        - name: teedy
          image: lovagesunny/teedy:v1.0
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: teedy-service
spec:
  type: NodePort
  selector:
    app: teedy-app
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30080
